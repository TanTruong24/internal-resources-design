services:
  - type: web
    name: vertical-v-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # set trong dashboard Render
